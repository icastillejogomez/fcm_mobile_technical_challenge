export type FCMPalette = {
  color: {
    primary: string
    secondary: string
    tertiary: string
    error: string
    warning: string
    success: string
    info: string
  }
  text: {
    default: string
    neutral: string
  }
  background: {
    primary: string
    header: string
  }
  tabBar: {
    background: string
    label: string
    icon: string
    labelTint: string
    iconTint: string
  }
}
